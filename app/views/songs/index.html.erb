<%= link_to "Sign Up", new_user_path %>

<% @songs.each do |song| %>
  <h3><%= song.title %></h3>
  <p>Length: <%= song.length %></p>
  <p>Play Count: <%= song.play_count %></p>
  <%= button_to 'Add song to cart', carts_path(song_id: song.id) %>
<% end %>
